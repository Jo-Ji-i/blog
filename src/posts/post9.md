---
title: 'React 전역 상태 비교'
date: '2025-03-08'
excerpt: 'Redux와 Zustand를 비교해보자.'
tags: ['React']
image: 'react.png'
category: 'react'
---

React로 프로젝트를 하다보면 많은 컴포넌트에서 관여하는 상태가 있는데

이를 전역으로 관리하게 된다.

실제로 Redux와 Zustand를 사용해본 입장에서 무엇이 다른지 정리하고자 한다.

## Redux — 체계적이지만 약간은 무겁다

Redux는 “예측 가능한 상태 관리(Predictable State Management)”를 표방하는,

전통 있고 가장 널리 쓰이는 라이브러리로 리액트와 리덕스는 한 세트 처럼 쓰이는 것 같다.

리 자 돌림 형제들…

### 기본 개념

-   모든 상태는 **단 하나의 전역 store**에 모임
-   상태 변경은 **action → reducer → store**의 단방향 흐름으로만 가능
-   상태는 **불변성(immutable)** 을 유지해야 함

### 구조 예시

```jsx
// store.js
import { createStore } from 'redux';

const initialState = { count: 0 };

function counterReducer(state = initialState, action) {
    switch (action.type) {
        case 'INCREMENT':
            return { count: state.count + 1 };
        default:
            return state;
    }
}

export const store = createStore(counterReducer);
```

```jsx
// Counter.js
import { useDispatch, useSelector } from 'react-redux';

export default function Counter() {
    const count = useSelector((state) => state.count);
    const dispatch = useDispatch();

    return (
        <>
            <p>{count}</p>
            <button onClick={() => dispatch({ type: 'INCREMENT' })}>+</button>
        </>
    );
}
```

### 장점

-   데이터 흐름이 **명확하고 예측 가능**
-   **디버깅 도구(Redux DevTools)** 지원이 아주 훌륭함
-   대규모 애플리케이션에서 구조적 관리에 강함

### 단점

-   보일러플레이트(보조 코드)가 많음
-   불변성 유지 등으로 코드가 장황해질 수 있음
-   작은 프로젝트에는 다소 **과한 구조**

> 🔸 Redux Toolkit (RTK)을 쓰면 훨씬 간결해지지만,
>
> 그래도 여전히 설정과 구조 설계가 필요.

## Zustand — 가볍고 직관적인 대안

Zustand는 독일어로 ‘상태(State)’를 의미해요.

이름처럼, **“상태를 단순하게 관리하자”**는 철학을 가지고있다.

Redux처럼 액션, 리듀서, 불변성 관리 이런 거 신경 쓸 필요 없다.

그냥 **store를 하나 만들고 바로 써요.**

### ⚙️ 기본 사용 예시

```jsx
// store.js
import { create } from 'zustand';

export const useCounterStore = create((set) => ({
    count: 0,
    increase: () => set((state) => ({ count: state.count + 1 })),
}));
```

```jsx
// Counter.js
import { useCounterStore } from './store';

export default function Counter() {
    const { count, increase } = useCounterStore();

    return (
        <>
            <p>{count}</p>
            <button onClick={increase}>+</button>
        </>
    );
}
```

이게 끝입니다.

`Provider`도 필요 없고, `reducer`도 없고, `dispatch`도 없어요.

### 장점

-   **설정이 거의 없음** (store 하나로 바로 사용 가능)
-   **보일러플레이트 최소화**
-   상태 변경 로직이 **한눈에 보임**
-   `useState`와 비슷한 사용감 → 진입 장벽 낮음
-   부분 구독(Selector)을 통해 **필요한 값만 리렌더링 가능**

### 단점

-   Redux만큼의 **공식 DevTools, 미들웨어 생태계**는 약함
-   상태 흐름이 자유로운 만큼 **통제력은 약함**
-   대규모 상태 구조 관리 시 명시적 패턴이 부족할 수 있음

## 결론

-   **Redux**는 "팀 단위 협업"에 강함
    상태 변경의 흐름이 명확하고, 누구나 코드를 봐도 이해할 수 있게 정형화되어 있어요.
-   **Zustand**는 "솔로 혹은 소규모 프로젝트"에 찰떡입니다.
    빠르게 상태를 구성하고, 유지보수가 간단해요.

> 개인적으로는,
>
> “서버 상태는 React Query, 클라이언트 전역 상태는 Zustand”
>
> 조합이 가장 실용적이었어요.
