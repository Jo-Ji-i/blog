import React from 'react';
import ThemeButton from './buttons/ThemeButton';

const Header = () => {
    return (
        <>
            <div>header</div>
            <ThemeButton />
        </>
    );
};

export default Header;
